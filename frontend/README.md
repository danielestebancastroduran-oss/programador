SIPO - Frontend (React + Bootstrap)

Instrucciones:

1. Instalar dependencias:

   npm install

2. Iniciar modo desarrollo:

   npm run dev

3. La app intentará conectarse al backend en http://localhost:3000/auth/login

Puedes cambiar la URL en `src/pages/Login.jsx` si el backend corre en otra dirección.
